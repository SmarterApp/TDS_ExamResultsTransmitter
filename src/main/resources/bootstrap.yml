spring:
  application:
    name: tds-exam-results-transmitter
  cloud:
    config:
      uri: ${CONFIG_SERVICE_URL:http://localhost:8888}
      enabled: ${CONFIG_SERVICE_ENABLED:false}
      fail-fast: ${CONFIG_SERVICE_ENABLED:false}
